# Support dashboard template

Tracked source trees used by the fixture generator. `base` is committed as `main`; each overlay is applied from that exact base. Overlay organization is fixture construction infrastructure, not production-readable feature ownership metadata.

