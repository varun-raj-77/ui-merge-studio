import { tickets } from '../../fixtures/tickets';
import { useSidebarState } from '../../hooks/useSidebarState';
import { SidebarNavItem } from './SidebarNavItem';
export function AppSidebar() { const { collapsed, toggle } = useSidebarState(); const open = tickets.filter(ticket => ticket.status === 'open').length; return <aside className={collapsed ? 'sidebar collapsed' : 'sidebar'}><div className="sidebar-top"><div className="brand" aria-label="Sample Support Desk">{collapsed ? 'S' : 'Sample Support Desk'}</div><button className="collapse-control" onClick={toggle} aria-expanded={!collapsed} aria-label={collapsed ? 'Expand sidebar' : 'Collapse sidebar'}>{collapsed ? '→' : '←'}</button></div>{!collapsed && <div className="demo-context">Demo application · Fake ticket data</div>}<nav aria-label="Primary"><SidebarNavItem label="Tickets" icon="◫" active collapsed={collapsed} badge={open} /><SidebarNavItem label="Customers" icon="◎" collapsed={collapsed} /><SidebarNavItem label="Reports" icon="▥" collapsed={collapsed} /></nav></aside>; }
