# Product Catalogue controlled fixture

The template generates a real React + TypeScript + Vite Git repository. `main` is the stable catalogue, `branch-a` adds a category sidebar plus an unrelated promotion, `branch-b` adds quick view plus unrelated sorting, and `branch-incompatible` migrates the shared product-ID contract.
