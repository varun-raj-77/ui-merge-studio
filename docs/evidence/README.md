# Evidence milestones

Public evidence is grouped by named engineering milestone so each directory explains the capability it proves. The `recorded-artifact-showcase` and `merge-lab-redesign` directories are the current recruiter-facing milestones.

Older `prompt-*` directories are retained as immutable historical evidence. Their names are legacy provenance, not runtime dependencies or the product's source of truth.
