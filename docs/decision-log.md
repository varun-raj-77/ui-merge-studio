# Decision log

- Keep the execution engine local because repository access, Git worktrees, package installation, source mutation, and verification cannot honestly run in a static hosted browser.
- Present the hosted experience as an interactive Product Catalogue sample backed by generated, committed controlled-engine evidence.
- Keep the baseline visible beside the focused branch on desktop and one action away on smaller screens.
- Start with an empty selection tray and allow changed regions to be selected or deselected in any order.
- Generate feature declarations, dependencies, exclusions, verification gates, and refusal records from engine output, then validate the typed replay manifest at build time.
- Replay success only for the recorded category-sidebar and quick-view pair.
- Refuse the numeric-versus-string product-ID conflict before mutation and label all other hosted combinations unrecorded.
- Keep generic runtime instrumentation and Git/AST analysis free of fixture component names.
- Use conservative refusal for unsupported source ownership, test slicing, imports, or overlapping declarations.
- Preserve Vite-only scope until a separately designed framework adapter proves another runtime.
- Keep preview context as serializable Studio-owned session state, separate from integration selections and candidate keys.
- Let the most recent intentional preview interaction own only the fields it changed; Studio-applied updates never echo as user intent.
- Negotiate route, viewport, normalized scroll, category, selected product, and Quick View against each preview's declared capabilities, preserving compatible fields and announcing deterministic fallbacks.
- Reapply the latest context after every preview-ready event and candidate artifact change. Discover rendered Quick View targets inside the repository adapter, not by scraping application DOM from the Studio shell.
- Describe every selectable visible change with a generic capability level, source branch, original route, page identity, support state, and optional evidence identity; keep Product Catalogue declarations in its adapter.
- Treat whole features, individual instances, and all-instances actions as different product operations. Expand an all-instances action atomically into existing instance scopes so candidate generation remains unchanged.
- Retain route and page ownership on integration selections and group the selection dock by that ownership. Features remain on their original routes; UI Merge Studio does not relocate or visually paste cross-route selections together.
- Store Category sidebar customization as one typed, serializable selection owned by its capability, source branch, route, and page. Its canonical identity is independent of checkbox click order and independent of the historical candidate key.
- Keep temporary category browsing separate from permanent sidebar source configuration. A configured result preserves a supported temporary category; otherwise it announces a fallback to the configured default without rewriting shared browsing state.
- Reuse the generic exported-const AST transform for configured features. Product Catalogue metadata owns category order, source path, and declaration name; malformed, stale, incompatible, or parentless configuration requests are refused.
- Keep the 64 recorded candidates as immutable historical proof and a structured parity oracle. Ordinary configured rendering consumes a canonical Integration Plan and does not resolve a candidate artifact.
- Keep category membership, permanent default, heading visibility, and product-count visibility in the same canonical Category sidebar decision. Appearance changes extend configuration identity and history equality, while candidate identity remains one of the same 64 keys.
- Derive category counts from the complete catalogue dataset, not the active filter. An explicitly enabled empty category displays `0` rather than disappearing.
- Store all integration intent in a generic, versioned canonical Integration Plan. Product Catalogue concepts live only in its adapter; complete plan snapshots drive history, while browsing context remains outside the plan.
- Expand bulk Quick View into canonical per-product decisions but record it as one atomic history transition. This retains exact target ownership without adding a second persistent bulk state.
- Derive preview, generation, verification, evidence, dock grouping, and conflict checks through explicit deterministic projections of the same validated plan.
- Version the canonical plan at 2 to add an explicit repository-scoped foundation, pinned foundation commit, pinned source commits, and verified common ancestor; version 1 is not silently reinterpreted.
- Treat a foundation as the complete starting branch, not a feature selection. Normalize redundant same-branch selections and preserve the prior safe plan when a foundation conflicts with existing decisions.
- Start generation from the exact pinned foundation worktree and integrate selected slices relative to the verified Git merge base. Reject stale refs, unrelated histories, and shared-contract incompatibility before successful candidate presentation.
- Make Integration Plan V2 the sole semantic authority for the normal local generation path. The browser submits only the canonical plan and its identity; it no longer submits feature-slice artifacts, analyzer metadata, source paths, components, or dependency lists for generation.
- Issue stable opaque selection capabilities from server analysis, but retain the full artifact and preview-session association only on the server. Recompute plan identity, compare every selection with current session-owned evidence, and refuse unknown, altered, stale, cross-session, repository, foundation, or commit mismatches before invoking candidate generation.
- Keep rendered-selection provenance server-resolvable before analysis: the preview Vite transform registers opaque receipt-to-source mappings over a session-private process channel, rendered messages contain only receipts, and the public analysis API refuses browser-authored `SourceIdentity`, paths, component names, and declaration locations.
- Define the Phase-0 trust boundary as one trusted local repository, developer, browser, and process environment. Selection receipts establish normal-flow/session provenance and remove browser-authored source metadata from analysis authority; they do not prove a physical click or protect against a developer recovering a registered receipt from transformed Vite source and directly calling localhost APIs.
- Keep the candidate generator unchanged behind a thin deterministic projection. Carry the one validated plan identity into preflight, the persisted generation report, verification evidence, and the generated-worktree preview state.
