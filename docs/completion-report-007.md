# Prompt 007 completion report

**Outcome: ABANDON.** FlowCraft is a healthy real application, but UI Merge Studio's preview, instrumentation, route-readiness, and synchronization stack is hard-wired to the Vite Support Dashboard fixture. FlowCraft uses Next.js 14 App Router. Because Prompt 007 explicitly excludes Next.js support, the experiment stopped at the first decisive architectural boundary instead of manufacturing a successful merge.

1. **Starting commits.** UI Merge Studio: `5e32bb0952c8ad8ef8e0c77c4ba60750f065064c` on `codex/phase0-final-product-experience`. FlowCraft: `261ab0f7c841278b0d68da7083955930980527fa` on `main`, tracking `origin/main`.
2. **FlowCraft path and architecture.** The inspected repository was `<FLOWCRAFT_REPOSITORY>`, not the empty OneDrive folder. It is an npm workspace with a Next.js 14/React 18 client, App Router, Zustand, `@xyflow/react`, Socket.IO client, Tailwind, React Hook Form/Zod, plus an Express/TypeScript, Socket.IO, Mongoose, JWT server.
3. **Validation branch model.** None was created. Existing `fix/*` branches are ancestors of `main`, do not form two independent visible variants, and cannot satisfy the prescribed model. Creating variants would still not make the Vite-only preview system able to instrument FlowCraft.
4. **Features chosen.** Candidate real features were execution-inspector/history work (`cc63387`, `ee7733b`) and replay/run-insight work (`d0f6bd7`). They already coexist on `main`; no legitimate independent branch pair was available.
5. **Unrelated changes.** None were inserted. No pre-existing FlowCraft change is claimed as an exclusion result because selection and slicing were never reached.
6. **Repository adapter/configuration.** No adapter or configuration was added. `server.ts` constructs a fixture-specific adapter and Vite preview controller; instrumentation exists only as a Vite plugin; readiness uses `/tickets`; synchronization reads `src/state/ticketSelection.ts`. Adding Next.js execution/instrumentation would violate scope.
7. **FlowCraft startup.** Its own deterministic frontend Playwright environment started successfully and passed 5/5 journeys using the repository's existing in-memory `/api` test adapter. This proves FlowCraft's frontend test startup, not UI Merge Studio preview startup or production backend behavior.
8. **Synchronization.** Not reached for FlowCraft. The current adapter only understands the Support Dashboard ticket route/state contract.
9. **Source mapping.** Not reached for rendered FlowCraft elements. The available transform and HTML bridge injection run only through Vite.
10. **Inspector dependencies.** Not analyzed from a visual FlowCraft selection.
11. **Replay dependencies.** Not analyzed from a visual FlowCraft selection.
12. **Compatibility.** Framework-level incompatibility was established before feature compatibility planning: Next.js App Router cannot pass through the current Vite-only preview/instrumentation path.
13. **Candidate generation/refusal.** No candidate was attempted or created. This is an architecture-level experiment abandonment, not a `PASS — bounded real-repository refusal`, because real rendering, mapping, and dependency analysis never generalized.
14. **Included FlowCraft changes.** None.
15. **Excluded FlowCraft changes.** None claimed.
16. **Verification.** FlowCraft passed client/server typechecks, client/server tests (157 total), production builds, and 5/5 client Playwright journeys. UI Merge Studio passed typecheck, 85/85 full tests, 27/27 focused studio tests, production build, fixture verification, and 4/4 focused browser scenarios.
17. **Visual result.** No FlowCraft result exists. Controlled-demo evidence in `docs/evidence/prompt-007/` confirms only the three bounded UI corrections and regression behavior.
18. **Causal-selection audit.** Not reached for FlowCraft; therefore no claim that a FlowCraft visual selection constrained a source plan.
19. **AI-agent baseline.** Execution was deferred because no valid independent branch inputs existed. `docs/ai-agent-benchmark.md` records the exact future prompt, inputs, measures, and refusal criteria; no fabricated comparison result is reported.
20. **Three UI corrections.** The combine tray is sticky and in document flow below previews; view and fit controls form a compact grouped header; the generated fixture is consistently branded **Sample Support Desk** with **Demo application · Fake ticket data**.
21. **UI Merge Studio files changed.** Product code: `apps/studio/src/{App.tsx,demoScenario.ts,studio.css}`. Fixture templates/verifier: four template files plus `scripts/verify-phase0-fixture.ts`. Tests: studio, fixture-contract, and guided-experience files. Documentation: the seven allowed existing documents and five required Prompt 007 documents. Prompt 007 screenshots were added. A pre-existing modified Prompt 006D screenshot was preserved and excluded from this work.
22. **FlowCraft files changed.** None. Its pre-existing untracked `work/` directory was preserved.
23. **Exact commands.** FlowCraft: `npm run typecheck`, `npm test`, `npm run build`, `npm run test:e2e --prefix client`. UI Merge Studio: `npm run typecheck`, `npm test`, `npm run test:studio`, `npm run build`, `npm run fixture:verify`, and `npx playwright test -c apps/studio/playwright.config.ts tests/e2e/guided-experience.spec.ts`.
24. **Tests/build results.** All commands in item 23 passed. The first focused browser attempt exposed one stale branding assertion (3/4 passed); after correcting only that assertion, the focused file passed 4/4. The final full UI Merge Studio suite took about 6m57s. No command timeout was treated as success.
25. **Source refs and cleanup.** FlowCraft remains on `main` at `261ab0f`; its three original local `fix/*` branches remain; no validation/candidate branch or extra worktree exists. UI Merge Studio has only its main worktree. Preview processes were terminated by the test runner.
26. **Remaining limitations.** Preview execution, source injection, readiness, route/state synchronization, and the fixture adapter are framework- and scenario-specific. Production FlowCraft also requires backend services and environment configuration not exercised by its mocked frontend E2E.
27. **Actually proven.** FlowCraft's native codebase is healthy; the current UI Merge Studio boundary is Vite/fixture-specific; the controlled demo remains green; and the three UI corrections work at 1280×720, 1440×900, and 1920×1080.
28. **Unproven.** Every real-repository product claim in the objective: dual FlowCraft previews, rendered element-to-source mapping, changed-region discovery, dependency slices, unrelated-edit exclusion, deterministic planning, candidate/refusal behavior after mapping, combined runtime, and selection-sensitive planning.
29. **Résumé readiness: NO.** Zero of the required real-FlowCraft evidence criteria were achieved. Existing controlled-fixture claims remain supportable, but Prompt 007 adds no honest real-repository résumé claim.
30. **Final recommendation: ABANDON.** Under the stated scope, the architecture does not survive contact with FlowCraft. A future experiment requires an explicit, separately scoped framework-neutral or Next.js preview/instrumentation architecture before repeating feature-level validation.
